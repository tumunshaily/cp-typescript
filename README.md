For running the main.ts use command bun cp
For running the main.ts in watch mode use bun cpw
For reading the input we have util for reading the input.txt file which exists in the util folder use them.
Cheers !
